machine:
  node:
    version: 4.8.2

dependencies:
  pre:
    - npm install -g npm@next

  post:
    - npm install --save prop-types 
  
deployment:
  staging:
    commands:
      - echo "Deploying to staging...."
